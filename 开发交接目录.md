# 诚信核查插件工具包开发交接记录

## 一、项目基本信息

项目名称：`integrity-screenshot-tool`

项目用途：辅助债券承做、尽调、诚信核查等工作中的网页截图整理。

项目定位：项目保留在同一个目录下，但拆成两个互不直接调用的子功能：

1. `extension/`：Edge 插件，只负责网站选择、打开网站、全屏截图、图片标注和保存截图。
2. `word_exporter/`：独立 Word 导出工具，用户完成全部截图后单独运行，后续可封装为 EXE。

明确不使用 Native Messaging；插件不直接调用 Python，也不直接调用 EXE。

---

## 二、当前项目结构

```text
integrity-screenshot-tool/
├─ extension/
│  ├─ manifest.json
│  ├─ popup.html
│  ├─ popup.js
│  ├─ background.js
│  ├─ content.js
│  └─ sites.json
│
├─ word_exporter/
│  ├─ generate_word.py
│  └─ post_process.py
│
├─ README.md
├─ run_generate_word.bat
└─ 开发交接目录.md
```

---

## 三、已完成功能

Edge 插件：

1. 读取 `sites.json` 网站清单。
2. 显示当前核查网站和进度。
3. 保存项目名称和核查主体名称。
4. 打开当前网站、上一个网站、下一个网站、跳过网站、重置进度。
5. 复制核查主体名称。
6. 选择整个屏幕后进行全屏截图。
7. 截图前临时隐藏插件弹窗，等待 0.8 秒后截图。
8. 在截图顶部添加核查网站、核查主体、核查时间、页面链接。
9. 保存截图到 Edge 下载目录下 `项目名称\核查主体名称\screenshots\`。

Word 导出工具：

1. 独立运行，不由插件触发。
2. 读取项目文件夹下各核查主体的 `screenshots/`。
3. 按截图文件名前缀序号排序。
4. 根据 `extension/sites.json` 匹配网站名称和页面链接。
5. 分别生成 Word 到各核查主体文件夹的 `report/`。
6. 已支持通过 PyInstaller 打包为 `dist\诚信核查截图Word导出工具.exe`，目标电脑无需安装 Python。
7. 导出工具可直接输入项目文件夹，也可输入 `screenshots` 文件夹并自动识别上一级项目。

---

## 四、保存路径逻辑

插件默认把截图保存到 Edge 默认下载目录下：

```text
C:\Users\你的用户名\Downloads\诚信核查插件工具包\项目名称\
└─ 核查主体名称\
   └─ screenshots\
      └─ 01_网站名称_核查主体名称_20260707_120000.png
```

Word 导出工具运行后输出：

```text
C:\Users\你的用户名\Downloads\诚信核查插件工具包\项目名称\
└─ 核查主体名称\
   └─ report\
      └─ 核查主体名称_诚信核查截图汇总.docx
```

---

## 五、使用流程

### 1. 加载插件

在 Edge 地址栏输入：

```text
edge://extensions/
```

打开开发人员模式，加载解压缩扩展，选择：

```text
integrity-screenshot-tool/extension
```

### 2. 使用插件截图

1. 打开插件。
2. 输入项目名称和核查主体名称。
3. 点击“新建项目”。
4. 点击“打开当前网站”。
5. 在网页中人工完成查询和判断。
6. 点击“保存当前截图”，选择“整个屏幕”。
7. 如误触跳过或切换，可点击“上一个网站”回退并重新打开。
8. 点击“下一个网站”继续。

### 3. 独立导出 Word

双击项目根目录下的：

```text
run_generate_word.bat
```

或运行：

```powershell
python word_exporter/generate_word.py
```

如果已经构建 EXE，也可以直接双击：

```text
dist\诚信核查截图Word导出工具.exe
```

按提示输入项目文件夹路径，例如：

```text
C:\Users\你的用户名\Downloads\诚信核查插件工具包\测试项目
```

---

## 六、当前不做的功能

1. 不自动识别核查结果。
2. 不判断是否存在风险。
3. 不生成核查结论。
4. 不自动处理验证码。
5. 不绕过网站限制。
6. 不使用 Native Messaging。
7. 插件不直接调用 Python。
8. 插件不直接调用 EXE。

---

## 七、下一步计划

1. 按业务场景继续维护 `extension/sites.json`。
2. 优化 Word 排版格式。
3. 继续测试 `word_exporter/` 的 EXE 在无 Python 电脑上的兼容性。
4. 增加不同核查场景的网站模板选择，例如发行人、担保人、重要子公司、董监高。
5. 研究插件打包方式，方便长期使用。
